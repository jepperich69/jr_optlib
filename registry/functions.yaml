# Vetted functions. Schema: registry/SCHEMA.md
# A `vetted` entry MUST list at least one oracle.

- id: transport.ipf_2d
  name: IPF / raking (2D)
  kind: function
  summary: Iterative Proportional Fitting -- KL I-projection of a seed onto given row/column marginals.
  tags: [transport, population-synthesis, matrix-scaling, ipf, furness, raking, entropy]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/population_transport.py (numerics-preserving extraction)
  added: 2026-07-02
  provenance: >
    Certified by two independent properties that jointly determine the unique
    solution (matrix scaling / Sinkhorn 1967): matching marginals
    (marginal_residual) and diagonal-scaling form (ipf_scaling_form). Also
    cross-checked against an independent log-space raking (ipf_reference).
    16 oracle-backed tests pass (tests/test_ipf.py).
  module: jr_optlib.transport.ipf
  signature: "ipf_2d(row_marg, col_marg, q=None, iters=1000, tol=1e-9) -> (X, elapsed)"
  oracles: [transport.marginal_residual, transport.ipf_scaling_form, transport.ipf_reference]
  tests: tests/test_ipf.py
  certifies: true

- id: transport.sinkhorn_balanced
  name: Sinkhorn (entropic OT, balanced)
  kind: function
  summary: Entropic optimal transport via Sinkhorn scaling of K=exp(-C/tau); returns the coupling.
  tags: [transport, optimal-transport, sinkhorn, entropic, matrix-scaling]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/population_transport.py (numerics-preserving migration)
  added: 2026-07-02
  provenance: >
    Migration verified bit-for-bit against the old paper copy on the paper's
    own instances (tests/test_sinkhorn.py::test_migration_matches_old_copy, 18
    cases). Certified by certify_sinkhorn: matching marginals + diagonal-scaling
    form relative to K=exp(-C/tau) uniquely determine the entropic-OT plan.
  module: jr_optlib.transport.sinkhorn
  signature: "sinkhorn_balanced(a, b, C, tau=0.05, iters=500, tol=1e-9) -> (X, elapsed)"
  oracles: [transport.certify_sinkhorn, transport.marginal_residual, transport.ipf_scaling_form]
  tests: tests/test_sinkhorn.py
  certifies: true

- id: transport.sinkhorn_balanced_uv
  name: Sinkhorn with scalings
  kind: function
  summary: As sinkhorn_balanced but also returns the (u, v) scaling vectors (for reduced-cost rounding).
  tags: [transport, optimal-transport, sinkhorn, entropic]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/population_transport.py (numerics-preserving migration)
  added: 2026-07-02
  provenance: Same algorithm/oracle as sinkhorn_balanced; returns (X, u, v, time).
  module: jr_optlib.transport.sinkhorn
  signature: "sinkhorn_balanced_uv(a, b, C, tau=0.05, iters=500, tol=1e-9) -> (X, u, v, elapsed)"
  oracles: [transport.certify_sinkhorn]
  tests: tests/test_sinkhorn.py
  certifies: true

- id: transport.make_transport
  name: Seeded balanced transport instance
  kind: function
  summary: Deterministic seeded generator of integer supply/demand (summing to N) and a cost matrix.
  tags: [transport, instance-generator, seeded, planted]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/population_transport.py
  added: 2026-07-02
  provenance: Guarantees supply.sum()==demand.sum()==N; deterministic given seed.
  module: jr_optlib.transport.sinkhorn
  signature: "make_transport(m, n, N, seed=1, cost_scale=1.0) -> TransportInstance"
  oracles: []
  tests: tests/test_sinkhorn.py
  certifies: false

- id: transport.solve_transport_opt
  name: Exact min-cost transport
  kind: function
  summary: Exact min-cost transportation LP (integral by TU); returns (X, obj, time).
  tags: [transport, min-cost-flow, exact, lp]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/population_transport.py (numerics-preserving migration)
  added: 2026-07-02
  provenance: >
    Migration verified vs old copy on objective+feasibility (LP is degenerate so
    the plan is not unique; cost is). Certified optimal by certify_transport
    against an independent scipy.linprog reference. Backend: ortools GLOP if
    available, else pulp/CBC.
  module: jr_optlib.transport.lp_transport
  signature: "solve_transport_opt(a, b, C) -> (X, obj, elapsed)"
  oracles: [transport.certify_transport, transport.transport_optimal_cost]
  tests: tests/test_lp_transport.py
  certifies: true

- id: transport.round_transport_min_cost_lp
  name: Integer transport via LP
  kind: function
  summary: Integerization by solving the min-cost transport LP (integral by TU); returns (Xint, time).
  tags: [transport, integerization, rounding, lp, exact]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/population_transport.py (numerics-preserving migration)
  added: 2026-07-02
  provenance: Verified vs old copy (objective+feasibility); certified optimal vs scipy reference.
  module: jr_optlib.transport.lp_transport
  signature: "round_transport_min_cost_lp(a, b, C) -> (Xint, elapsed)"
  oracles: [transport.certify_transport]
  tests: tests/test_lp_transport.py
  certifies: true

- id: transport.round_transport_min_cost_lp_restricted
  name: Integer transport LP on restricted support
  kind: function
  summary: Min-cost transport LP over a boolean arc mask E; returns (Xint, time, ok) with full-LP fallback signal.
  tags: [transport, integerization, rounding, lp, restricted]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/population_transport.py (numerics-preserving migration)
  added: 2026-07-02
  provenance: Helper for the greedy-push repair; feasibility self-checked (returns ok=False if marginals unmet).
  module: jr_optlib.transport.lp_transport
  signature: "round_transport_min_cost_lp_restricted(a, b, C, E) -> (Xint, elapsed, ok)"
  oracles: [transport.certify_transport]
  tests: tests/test_lp_transport.py
  certifies: false

- id: transport.round_transport_greedy_push
  name: Reduced-cost greedy transport rounding
  kind: function
  summary: Greedy integer push by Sinkhorn reduced cost, with restricted-LP repair and full-LP fallback.
  tags: [transport, integerization, rounding, heuristic, greedy]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/population_transport.py:882 (ACTIVE def; :789 dead-shadowed copy dropped)
  added: 2026-07-02
  provenance: >
    Resolves the double-definition bug (two same-named functions in the paper
    file; only :882 was live). Verified vs the live/old copy on
    objective+feasibility+ok. Heuristic: certify_transport(require_optimal=False)
    yields CHECKED (feasible + cost >= optimum), not certified.
  module: jr_optlib.transport.lp_transport
  signature: "round_transport_greedy_push(a, b, C, u, v, tau, topk=None) -> (Xint, elapsed, ok)"
  oracles: [transport.certify_transport]
  tests: tests/test_lp_transport.py
  certifies: false

- id: transport.round_transport_min_cost_mcf
  name: Min-cost-flow integer transport
  kind: function
  summary: Integer transport via OR-Tools min-cost flow (TU); falls back to the LP rounder when the graph solver is unavailable.
  tags: [transport, integerization, rounding, min-cost-flow, exact]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/population_transport.py (numerics-preserving migration)
  added: 2026-07-02
  provenance: Verified vs old copy (objective+feasibility); optimal -> CERTIFIED by certify_transport vs scipy.
  module: jr_optlib.transport.rounding
  signature: "round_transport_min_cost_mcf(a, b, C, topk=None) -> (Xint, elapsed)"
  oracles: [transport.certify_transport]
  tests: tests/test_rounding.py
  certifies: true

- id: transport.round_transport_min_cost_approx
  name: Restricted-support approximate transport rounder
  kind: function
  summary: Restricted-support LP over top-k-by-cost arcs (+ high-mass Sinkhorn arcs); full-LP fallback. Returns (Xint, time, used_restricted).
  tags: [transport, integerization, rounding, heuristic, restricted]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/population_transport.py (numerics-preserving migration)
  added: 2026-07-02
  provenance: Verified vs old copy (objective+feasibility+flag). Feasible; certify_transport(require_optimal=False) -> CHECKED (restricted support may be suboptimal).
  module: jr_optlib.transport.rounding
  signature: "round_transport_min_cost_approx(a, b, C, Xtau=None, topk=30, eta=0.0) -> (Xint, elapsed, used_restricted)"
  oracles: [transport.certify_transport]
  tests: tests/test_rounding.py
  certifies: false

- id: transport.round_transport_floor_residue_lp
  name: Floor-residue LP alias
  kind: function
  summary: Backward-compatible alias for round_transport_min_cost_lp (kept from the paper API).
  tags: [transport, integerization, rounding, alias]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/population_transport.py
  added: 2026-07-02
  provenance: Delegates to round_transport_min_cost_lp; same certificate.
  module: jr_optlib.transport.rounding
  signature: "round_transport_floor_residue_lp(a, b, C) -> (Xint, elapsed)"
  oracles: [transport.certify_transport]
  tests: tests/test_rounding.py
  certifies: true

- id: transport.dependent_round_2d
  name: Dependent rounding of a 2D table (dispatcher)
  kind: function
  summary: Round a fractional 2D table (e.g. IPF fit) to integers preserving both marginals, per-cell deviation < 1. Dispatches mcf/lp/sparsified.
  tags: [population-synthesis, integerization, dependent-rounding, contingency]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/population_transport.py (was private _dependent_round_2d_*; public here)
  added: 2026-07-02
  provenance: >
    Completes the ipf_2d -> integer pipeline. Contract (exact marginals +
    integrality + per-cell deviation < 1) certified by certify_dependent_round.
    Migration verified vs the old private helpers on contract + residual
    objective (residual LP has tied optima -> plan not unique).
  module: jr_optlib.transport.population_round
  signature: "dependent_round_2d(X, row_marg, col_marg, method='mcf', sparsify=False, ...) -> (Xint, elapsed)"
  oracles: [transport.certify_dependent_round]
  tests: tests/test_population_round.py
  certifies: true

- id: transport.dependent_round_2d_lp
  name: Dependent rounding (dense LP)
  kind: function
  summary: Residual 0-1 LP rounding of a fractional table (TU -> integral); exact marginals, per-cell deviation < 1.
  tags: [population-synthesis, integerization, dependent-rounding, lp]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/population_transport.py (was _dependent_round_2d_lp)
  added: 2026-07-02
  provenance: Certified by certify_dependent_round; migration verified vs old copy on contract + residual objective.
  module: jr_optlib.transport.population_round
  signature: "dependent_round_2d_lp(X, row_marg, col_marg) -> (Xint, elapsed)"
  oracles: [transport.certify_dependent_round]
  tests: tests/test_population_round.py
  certifies: true

- id: transport.dependent_round_2d_lp_sparsified
  name: Dependent rounding (sparsified LP)
  kind: function
  summary: Sparsified residual LP (top-K fractional + eta-prune) with dense-LP fallback; same contract as the dense version.
  tags: [population-synthesis, integerization, dependent-rounding, lp, sparsified]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/population_transport.py (was _dependent_round_2d_lp_sparsified)
  added: 2026-07-02
  provenance: Certified by certify_dependent_round.
  module: jr_optlib.transport.population_round
  signature: "dependent_round_2d_lp_sparsified(X, row_marg, col_marg, topk_extra=8, eta=0.02, eps_slack=0.0) -> (Xint, elapsed)"
  oracles: [transport.certify_dependent_round]
  tests: tests/test_population_round.py
  certifies: true

- id: transport.dependent_round_2d_mcf
  name: Dependent rounding (min-cost flow)
  kind: function
  summary: Residual 0-1 min-cost flow rounding via OR-Tools; dense-LP fallback when the graph solver is unavailable.
  tags: [population-synthesis, integerization, dependent-rounding, min-cost-flow]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/population_transport.py (was _dependent_round_2d_mcf)
  added: 2026-07-02
  provenance: Certified by certify_dependent_round.
  module: jr_optlib.transport.population_round
  signature: "dependent_round_2d_mcf(X, row_marg, col_marg, cost_scale=200000, topk_extra=8) -> (Xint, elapsed)"
  oracles: [transport.certify_dependent_round]
  tests: tests/test_population_round.py
  certifies: true

- id: transport.make_contingency2d
  name: Synthetic 2D marginals
  kind: function
  summary: Deterministic seeded generator of feasible row/column marginals (planted instance, oracle type 4).
  tags: [transport, population-synthesis, instance-generator, seeded]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/population_transport.py
  added: 2026-07-02
  provenance: >
    Guarantees row.sum() == col.sum() == N (asserted), the feasibility
    precondition for IPF. Deterministic given seed.
  module: jr_optlib.transport.ipf
  signature: "make_contingency2d(R, C, N, seed=1, skew=1.2) -> Contingency2D"
  oracles: []
  tests: tests/test_ipf.py
  certifies: false

- id: setcover.solve_entropy_setcover
  name: Entropy-guided set-cover heuristic
  kind: function
  summary: RICH set-cover core: row-wise entropy relaxation followed by dual-guided rounding and drop-fix pruning.
  tags: [set-cover, entropy, heuristic, rich, rounding]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/synth_setcover.py (Gurobi-free core; numerics-preserving migration)
  added: 2026-07-02
  provenance: >
    Migration verified against the active paper copy on seeded synthetic
    instances with polish disabled (polish_time=0, do_polish_mh=False).
    This first slice deliberately excludes Gurobi restricted-polish and MH
    polish. Oracle coverage: certify_setcover_solution recomputes feasibility
    and objective from the selected columns; CHECKED, not optimality-certified.
  module: jr_optlib.setcover.entropy
  signature: "solve_entropy_setcover(A_matrix, c, tau=0.1, iters=50, tol=1e-3, tau_schedule=None, polish_time=0.0, ...) -> (x_int, cost, feasible, elapsed)"
  oracles: [setcover.certify_setcover_solution, setcover.setcover_feasible, setcover.setcover_cost]
  tests: tests/test_setcover_entropy.py
  certifies: false

- id: setcover.gen_entropy_friendly_scp
  name: Synthetic entropy-friendly set-cover generator
  kind: function
  summary: Deterministic seeded generator for the MIPEntropy synthetic set-cover instances.
  tags: [set-cover, instance-generator, seeded, synthetic]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/synth_setcover.py
  added: 2026-07-02
  provenance: >
    Migration verified bit-for-bit against the paper copy for seeded cases.
    Guarantees every row has at least one covering column.
  module: jr_optlib.setcover.entropy
  signature: "gen_entropy_friendly_scp(n, m, seed=42) -> (A, c)"
  oracles: [setcover.matrix_to_covers]
  tests: tests/test_setcover_entropy.py
  certifies: false

- id: setcover.polish_solution
  name: Restricted Gurobi set-cover polish
  kind: function
  summary: Refines a feasible set-cover solution by freeing uncertain variables in a restricted Gurobi MIP.
  tags: [set-cover, gurobi, polish, restricted-mip, heuristic]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/synth_setcover.py (numerics-preserving migration)
  added: 2026-07-02
  provenance: >
    Migration verified against the active paper copy on a seeded synthetic
    instance with identical warm start and fractional guide. Oracle coverage:
    certify_setcover_solution recomputes feasibility and objective. Gurobi
    license verified on this machine 2026-07-02.
  module: jr_optlib.setcover.entropy
  signature: "polish_solution(x_int, A, c, polish_time=1.0, polish_pool=0.3, x_frac=None) -> (x_polished, cost_polished, elapsed)"
  oracles: [setcover.certify_setcover_solution]
  tests: tests/test_setcover_entropy.py
  certifies: false

- id: setcover.solve_mip
  name: Gurobi set-cover MIP solver
  kind: function
  summary: Exact/time-limited Gurobi set-cover solve with objective bound and optional incumbent tracking.
  tags: [set-cover, gurobi, exact, mip, benchmark]
  status: vetted
  source: Pub_MIPEntropy_MPC/code/mip_hybrid/apps/synth_setcover.py (numerics-preserving migration)
  added: 2026-07-02
  provenance: >
    Migration verified against the active paper copy on a seeded synthetic
    instance. Returned objective, dual bound, and gap match. Optimal rows can
    be CERTIFIED by gap_certificate; time-limited rows are CHECKED by bound
    consistency.
  module: jr_optlib.setcover.entropy
  signature: "solve_mip(A, c, timelimit_s=30, gurobi_time_limit=None, gurobi_gap_limit=None, track_gurobi_anytime=False) -> dict"
  oracles: [core.gap_certificate, setcover.certify_setcover_solution]
  tests: tests/test_setcover_entropy.py
  certifies: true

- id: population.HardIPF
  name: Hard-coded high-dimensional IPF
  kind: function
  summary: Pandas-backed N-dimensional IPF over a long population table and arbitrary marginal constraints.
  tags: [population-synthesis, ipf, raking, high-dimensional, pandas, popint]
  status: vetted
  source: Pub_PopInt_PartB/Large Scale/Intege_Paper_Minimal_SWAP_ARC.py (numerics-preserving migration)
  added: 2026-07-02
  provenance: >
    Migration verified bit-for-bit against the active PopInt large-scale script
    on a synthetic multi-margin zone. Certified by
    certify_population_margins: every fitted weighted marginal matches the
    supplied target constraints within tolerance. This is the first PopInt
    integration slice; anchor PPS and swap-repair remain paper-local.
  module: jr_optlib.population.hard_ipf
  signature: "HardIPF(seed_df, constraints, weight_col='x').fit(tol=1e-7, max_iters=200, verbose=True) -> (w, ok, n_iter, max_rel_err)"
  oracles: [population.certify_population_margins]
  tests: tests/test_population_hard_ipf.py
  certifies: true

- id: population.swap_repair_zone
  name: Anchor-preserving population swap repair
  kind: function
  summary: Repairs one zone's integer population table by moving mass within Age x Gender slices to satisfy non-anchor margins.
  tags: [population-synthesis, integerization, swap-repair, anchor-preserving, pandas, popint]
  status: vetted
  source: Pub_PopInt_PartB/Large Scale/Intege_Paper_Minimal_SWAP_ARC.py (numerics-preserving migration)
  added: 2026-07-02
  provenance: >
    Migration verified against the active PopInt large-scale script on a
    synthetic zone where the repair performs an Age x Gender preserving swap.
    Certified on the test instance by certify_population_margins over the
    integer n column; in general this repair is CHECKED because feasibility
    depends on the supplied integer residual structure.
  module: jr_optlib.population.integerize
  signature: "swap_repair_zone(int_df_zone, frac_df_zone, constraints, max_passes=3, max_moves_per_slice=2000, ghost_x_eps=1e-6, anchor_name='Age x Gender') -> (int_df_zone, stats)"
  oracles: [population.certify_population_margins]
  tests: tests/test_population_hard_ipf.py
  certifies: false

- id: population.step1_split
  name: Deterministic population floor split
  kind: function
  summary: Splits one zone's fractional population weights into integer floors, fractional residues, and remaining additions.
  tags: [population-synthesis, integerization, floor-split, pandas, popint]
  status: vetted
  source: Pub_PopInt_PartB/Large Scale/Intege_Paper_Minimal_SWAP_ARC.py (numerics-preserving migration)
  added: 2026-07-02
  provenance: >
    Migration verified against the active PopInt large-scale script on a
    synthetic zone. This is a deterministic precondition step for anchor PPS
    and swap repair.
  module: jr_optlib.population.integerize
  signature: "step1_split(frac_df_zone, zone_target) -> (df_split, k_add)"
  oracles: [population.certify_population_margins]
  tests: tests/test_population_hard_ipf.py
  certifies: false

- id: population.step2_anchor_pps
  name: Anchor-conditioned PPS integer additions
  kind: function
  summary: Samples residual integer additions within Age x Gender anchor slices using fractional residues as PPS weights.
  tags: [population-synthesis, integerization, pps, anchor-preserving, pandas, popint]
  status: vetted
  source: Pub_PopInt_PartB/Large Scale/Intege_Paper_Minimal_SWAP_ARC.py (numerics-preserving migration)
  added: 2026-07-02
  provenance: >
    Migration verified against the active PopInt large-scale script with a
    fixed RNG seed, including identical selected-row records. Certified on the
    test instance by certify_population_margins over the integer n column for
    the anchor constraint; in general this stochastic primitive is CHECKED.
  module: jr_optlib.population.integerize
  signature: "step2_anchor_pps(df_split, constraints, anchor_dims, k_add, seed=42, anchor_name='Age x Gender') -> (df, chosen_df)"
  oracles: [population.certify_population_margins]
  tests: tests/test_population_hard_ipf.py
  certifies: false
